name: DAST – OWASP ZAP Positive Scan

on:
  push:
    branches:
      - feature/dast-setup
  pull_request:
    branches:
      - main

jobs:
  zap_positive:
    name: OWASP ZAP Positive Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4

      - name: Start local HTTP server
        run: |
          python3 -m http.server 8080 &
          sleep 5

      - name: Run ZAP full scan (positive)
        uses: zaproxy/action-full-scan@v0.5.0
        continue-on-error: true        # ← allow this step to *not* block the job
        with:
          target: 'http://localhost:8080/index.html'
          docker_name: 'ghcr.io/zaproxy/zaproxy:stable'
          fail_action: 'false'
          allow_issue_writing: 'false'

      - name: Show ZAP report snippet
        if: always()
        run: |
          echo "===== ZAP HTML Report (first 50 lines) ====="
          head -n 50 report_html.html || echo "(report_html.html missing)"
          echo "============================================="
