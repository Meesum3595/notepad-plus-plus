name: DAST – OWASP ZAP Full Scan

on:
  pull_request:
    branches:
      - main

jobs:
  zap_scan:
    name: OWASP ZAP Full Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4

      - name: Start local HTTP server
        run: |
          python3 -m http.server 8080 &
          sleep 5

      - name: Run ZAP full scan (positive case)
        uses: zaproxy/action-full-scan@v0.5.0
        with:
          target: 'http://localhost:8080/index.html'
          docker_name: 'ghcr.io/zaproxy/zaproxy:stable'
          # don’t fail on any alert → lets WARN‑NEW pass
          fail_action: false

      - name: Show ZAP HTML report snippet
        if: always()
        run: |
          echo "===== ZAP HTML Report (first 100 lines) ====="
          head -n 100 report_html.html || echo "(no report_html.html found)"
          echo "=============================================="
